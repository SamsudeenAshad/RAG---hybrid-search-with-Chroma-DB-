# ---- LLM provider ----
# Which provider drives the agent reasoning: "gemini", "ollama", or "nvidia".
LLM_PROVIDER=gemini

# ---- Embedding provider ----
# "gemini", "ollama", or "nvidia". Each provider uses its OWN Chroma collection
# (<COLLECTION_BASE>_<provider>) sized to its vector dimension, so you must
# query with the same provider you ingested with.
EMBED_PROVIDER=gemini

# ---- Google Gemini (REQUIRED for embeddings; also default chat provider) ----
# Get a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-gemini-api-key

# ---- Ollama (self-hosted; used when LLM_PROVIDER/EMBED_PROVIDER=ollama) ----
# OLLAMA_MODEL drives chat AND reasoning. OLLAMA_EMBED_MODEL must be a real
# embedding model pulled on the host (a chat model like gemma cannot embed).
# Both models must already be pulled on the Ollama host (`ollama pull <name>`).
OLLAMA_BASE_URL=http://your-ollama-host:11434
OLLAMA_MODEL=gemma4:e4b
OLLAMA_EMBED_MODEL=nomic-embed-text

# ---- NVIDIA NIM (hosted; used when LLM_PROVIDER=nvidia) ----
# Hosted chat models via the OpenAI-compatible endpoint. Chat only (no
# embeddings). Get a key at https://build.nvidia.com (nvapi-...).
NVIDIA_API_KEY=
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_MODEL=meta/llama-3.3-70b-instruct
# Embeddings (1024-dim) when EMBED_PROVIDER=nvidia. Gets its own Chroma
# collection; re-ingest documents under EMBED_PROVIDER=nvidia to use it.
NVIDIA_EMBED_MODEL=nvidia/nv-embedqa-e5-v5

# ---- Gemini models ----
GEMINI_CHAT_MODEL=gemini-2.5-flash
# 'pro' needs a paid Gemini key (free tier quota = 0). Use flash on free tier.
GEMINI_REASONING_MODEL=gemini-2.5-flash
GEMINI_EMBED_MODEL=models/gemini-embedding-001
EMBED_DIM=3072

# ---- Chroma Cloud ----
# Run `chroma login` then `chroma db create chroma_rag`. Find the tenant id in
# ~/.chroma/credentials. The collection base gets a per-provider suffix
# (e.g. documents_gemini).
CHROMA_TENANT=
CHROMA_DATABASE=chroma_rag
CHROMA_API_KEY=
COLLECTION_BASE=documents

# ---- PostgreSQL ----
# Used for LangGraph checkpoints + document/chunk metadata
DATABASE_URL=postgresql://a2z:a2z@localhost:5442/chroma_rag

# ---- Retrieval / graph tuning ----
RETRIEVAL_TOP_K=20
RERANK_TOP_N=6
SCORE_THRESHOLD=0.45
MAX_ATTEMPTS=2

# ---- Web search (keyless DuckDuckGo by default) ----
# Set WEB_SEARCH_PROVIDER=tavily and TAVILY_API_KEY=... to use Tavily instead.
WEB_SEARCH_PROVIDER=duckduckgo
TAVILY_API_KEY=

# ---- Reranker (keyless local cross-encoder via fastembed) ----
RERANK_MODEL=Xenova/ms-marco-MiniLM-L-6-v2
SPARSE_MODEL=Qdrant/bm25

# ---- LangGraph ----
LANGGRAPH_STRICT_MSGPACK=true
